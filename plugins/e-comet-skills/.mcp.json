{
  "mcpServers": {
    "e-comet": {
      "type": "http",
      "url": "https://mcp.e-comet.io/mcp"
    },
    "e-comet-local": {
      "type": "stdio",
      "command": "cmd.exe",
      "args": [
        "/d",
        "/s",
        "/c",
        "if defined CLAUDE_PLUGIN_ROOT (call \"%CLAUDE_PLUGIN_ROOT%\\mcp\\launch-windows.cmd\") else (call \"mcp\\launch-windows.cmd\")"
      ],
      "cwd": "."
    }
  }
}
