---
name: wb-product-images
id: wb-product-images
title: Фото товаров WB
description: "Находит доступные URL фото товаров Wildberries по артикулам WB. Используйте, когда пользователь просит картинки WB, фото артикула, URL фото по product_id / nm_id / nmId / SKU или пакетную выгрузку ссылок на фото товаров Вайлдберриз."
skill_schema_version: 1
introduced_in: "2026.6.0"
updated_in: "2026.7.6"
status: stable
---

# Фото товаров WB

## Workflow

1. Разбейте артикулы на группы до 20 и вызовите локальный MCP tool `wb_product_images` для каждой группы. Для 1–20 артикулов достаточно одного вызова.
2. При необходимости задайте:
   - `maxPhotos` — от 1 до 30, по умолчанию 15;
   - `size` — `big` или `tm`, по умолчанию `big`;
   - `maxBasket` — глубина fallback-поиска CDN, по умолчанию 60.
   - `timeout` — положительный таймаут одной проверки в миллисекундах.
3. Верните `products[].imageUrls`. Полный компактный результат также сохранён локально по `resultPath`.

Tool обращается прямо к публичному image-CDN Wildberries. Для него не требуется подключение расширения, Python или backend e-Comet.
Используйте результат tool, а не собирайте и не угадывайте итоговые URL вручную.

## Ответ пользователю

- Укажите количество найденных фото по каждому артикулу.
- Для нескольких групп объедините результаты в исходном порядке и явно перечислите отклонённые некорректные значения.
- При `status: "not_found"` говорите: «фото не найдены текущей проверкой image-CDN».
- Не утверждайте, что товар не существует на WB только из-за отсутствия фото.
