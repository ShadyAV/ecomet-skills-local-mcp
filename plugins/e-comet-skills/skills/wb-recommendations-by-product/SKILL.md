---
name: wb-recommendations-by-product
id: wb-recommendations-by-product
title: Рекомендации WB по товару
description: "Получает живые рекомендательные полки Wildberries для одного или нескольких артикулов через браузер пользователя. Используйте, когда пользователь спрашивает, какие товары WB рекомендует рядом с заданным товаром или встречается ли конкретный артикул в рекомендациях другого."
skill_schema_version: 1
introduced_in: "2026.7.6"
updated_in: "2026.7.6"
status: experimental
---

# Рекомендации WB по товару

## Workflow

**Главное правило:** вызовите `browser_job` ровно один раз и сразу выполните полученную авторизацию через `execute_browser_job`. Не декодируйте JWT и не воспроизводите `trigger_url` текстом.

0. **Только для Claude:** обязательный первый шаг — фактически вызвать `browser_job` по шагу 1. Служебные статусы клиента и отсутствие tool в текущем списке не доказывают проблему с авторизацией и не отменяют вызов. Если tool не виден, подождите несколько секунд и повторите поиск до 3 раз, затем всё равно попытайтесь вызвать его. Говорите об авторизации только после соответствующей ошибки фактического вызова `browser_job` или `execute_browser_job`; иначе вернитесь к шагу 1. Если фактический вызов завершился ошибкой, сообщите пользователю только подтверждённую ею причину понятным языком, без названий tools и внутренних статусов. Не добавляйте догадок и рекомендаций, не следующих из ошибки.

Передавайте до 20 артикулов. `{nm}` без `pages` означает всю обнаруженную полку (до 60 страниц), `{nm, pages}` — заданную глубину до 60 страниц.

**Codex/ChatGPT Desktop:** выполните оба MCP-вызова внутри одного `exec`. Не разделяйте их между разными `exec`, не вызывайте `store()` и не выводите `triggerUrl` через `text()`:

```js
const authorization = await tools.mcp__e_comet__browser_job({
    job: {
        type: 'recommendations_by_product',
        articles: [
            { nm: 791050753, pages: 1 },
            { nm: 913357757, pages: 5 },
            { nm: 1244492882 },
        ],
    },
});
if (authorization.isError) {
    text(authorization);
    exit();
}
const triggerUrl = authorization.structuredContent?.trigger_url;
if (typeof triggerUrl !== 'string') {
    text(JSON.stringify({ ok: false, code: 'BROWSER_JOB_TRIGGER_MISSING' }));
    exit();
}
const result = await tools.mcp__e_comet_local__execute_browser_job({
    triggerUrl,
    productLimitTotal: 20,
});
text(result);
```

Если подключён stage-сервер, используйте соответствующее доступное имя `browser_job` вместо `mcp__e_comet__browser_job`. Для поиска конкретных товаров замените или дополните `productLimitTotal` полем `productNmIds`. Токен должен существовать только в локальной JS-переменной этого `exec`.

**Claude Cowork и Claude Code CLI:** вызовите удалённый `browser_job` с тем же descriptor, затем сразу вызовите `execute_browser_job` без `triggerUrl`, используя только нужную проекцию:

```json
{ "productLimitTotal": 20 }
```

Desktop hook подставит точное значение автоматически. Служебные статусы клиента и отсутствие tool в текущем списке не доказывают проблему с авторизацией. Если tool не виден, подождите несколько секунд и повторите поиск до 3 раз, затем всё равно попытайтесь сделать фактический вызов. Сообщайте только подтверждённую фактическим вызовом причину понятным языком, без внутренних названий tools и недоказанных рекомендаций.

Используйте `articles[].pages[].products`; полные WB-ответы сохранены локально по `resultPath`.

`browser_job` передаёт backend только описание разрешённого задания и возвращает подписанный JWT с ограниченным сроком действия. Ответы WB через backend не идут. Не декодируйте JWT, не открывайте вкладки и не управляйте DOM/CDP.

Если расширение не подключено, вызовите `local_bridge_status` и попросите пользователя открыть Chrome с обновлённым расширением e-Comet и хотя бы одной авторизованной вкладкой `wildberries.ru`.

`trigger_url` одноразовый. Если `browser_job` не вернул авторизацию из-за ошибки в аргументах, исправьте аргументы и повторите весь flow один раз. Ошибку handoff или подписи автоматически не обходите и не повторяйте с вручную переданным токеном; покажите подтверждённую причину пользователю.

`resultPath` не является кэшем. Читайте только путь, возвращённый текущим `execute_browser_job`, и только если компактного `articles` не хватило для ответа. Чтение `resultPath` не требует повторного `browser_job` или `execute_browser_job`. Не ищите и не переиспользуйте файлы прошлых пользовательских заданий.

Каждая строка NDJSON в `resultPath` имеет вид `{ jobId, nmId, page, url, response }`. Исходное тело WB находится в `response.data.body`.

## Ответ пользователю

- Группируйте результат по `sourceNmId`.
- Используйте готовую сквозную позицию `globalPosition`; `position` — место на странице.
- Для списка показывайте артикул, название, бренд, `priceRub.product`, рейтинг и отзывы.
- Помечайте рекламу только при `promoted: true`.
- При статусе `partial` или `failed` не утверждайте полноту полки; укажите число успешных и неуспешных страниц.
- При `truncatedByLocalLimit: true` полка длиннее локального предела в 60 страниц: явно скажите, что результат обрезан, даже если все запрошенные страницы получены успешно.
- Рекомендации — снимок на момент запроса и могут меняться.
